# Telegram Gemini Bot

Telegram бот с поддержкой Gemini 2.5/2.0 Flash, работающий через Google GenAI SDK.

## Возможности

- 💬 Текстовые запросы с историей диалога
- 🖼️ Анализ изображений
- 🔄 Автоматическое переключение между API ключами при лимитах
- 📊 Автопосты с дайджестами (опционально)
- ⚙️ Настраиваемый размер ответов

## Развертывание на Render

### 1. Подготовка

1. Скопируйте файлы в отдельную папку:
   - `сигмоида.py`
   - `requirements.txt`
   - `render.yaml` (опционально)

### 2. Создание сервиса на Render

1. Зайдите на [Render Dashboard](https://dashboard.render.com)
2. Нажмите "New +" → "Background Worker"
3. Подключите GitHub репозиторий (или используйте public Git)
4. Заполните настройки:
   - **Name**: `telegram-gemini-bot`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python сигмоида.py`

### 3. Переменные окружения

Добавьте следующие переменные в разделе "Environment":
- `TG_TOKEN` - токен вашего Telegram бота от @BotFather
- `GEMINI_API_KEY_1` - первый Gemini API ключ
- `GEMINI_API_KEY_2` - второй Gemini API ключ

### 4. Деploy

Нажмите "Create Background Worker" и дождитесь завершения деплоя.

## Команды бота

- `/start` - начать работу
- `/help` - справка
- `/settings` - текущие настройки
- `/reset` - очистить историю
- `/autopost on|off` - автопосты (только админы)
- `/set_interval <сек>` - интервал автопоста
- `/set_minmsgs <n>` - минимум сообщений для автопоста
- `/set_msgsize small|medium|large` - размер ответов

## Локальный запуск

```bash
cd c:\Users\user\Desktop\сигмоида
pip install -r requirements.txt
set TG_TOKEN=your_token_here
set GEMINI_API_KEY_1=your_key_1
set GEMINI_API_KEY_2=your_key_2
python сигмоида.py
```

## Структура

- `сигмоида.py` - основной файл бота
- `requirements.txt` - зависимости Python
- `render.yaml` - конфигурация Render (опционально)
- `README.md` - документация

## Примечания

- Бот использует модель `gemini-2.5-flash` по умолчанию
- При лимите API автоматически переключается на `gemini-2.0-flash`
- История диалога сохраняется в памяти (сбрасывается при рестарте)
- Для обработки изображений используется библиотека Pillow

